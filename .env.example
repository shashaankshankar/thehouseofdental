# Configure these in Vercel with environment-specific values.
# Never commit values from production or an approved notification/CRM service.
GOOGLE_PLACE_ID=
GOOGLE_PLACES_API_KEY=

# The appointment adapter fails closed until all three values are configured.
APPOINTMENT_BACKEND_URL=
APPOINTMENT_BACKEND_TOKEN=
APPOINTMENT_ALLOWED_ORIGINS=
