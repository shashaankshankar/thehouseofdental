{
  "name": "the-house-of-dental-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "clean": "node scripts/clean.mjs",
    "validate:measurement": "node scripts/validate-measurement.mjs",
    "validate": "node scripts/validate.mjs dist",
    "validate:vercel": "node scripts/validate-vercel.mjs",
    "test": "node --test tests/site.test.mjs tests/vercel-functions.test.mjs",
    "check": "npm run build && npm run validate:measurement && npm run validate && npm test && npm run validate:vercel && node --check api/google-reputation.js && node --check api/appointment.js && node --check dist/main.js"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1"
  }
}
